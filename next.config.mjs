/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "vimsxaud7lt1te1j.public.blob.vercel-storage.com" },
    ],
  },
};

export default nextConfig;
